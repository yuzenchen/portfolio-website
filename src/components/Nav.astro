---
const links = [
  { href: '#services', label: '/services' },
  { href: '#work', label: '/work' },
];
---
<nav>
  <div class="brand">yuzen@tw:~$</div>
  <div class="links">
    {links.map(({ href, label }) => <a href={href}>{label}</a>)}
    <a href="#contact" class="btn-outline-accent">/contact</a>
  </div>
</nav>
